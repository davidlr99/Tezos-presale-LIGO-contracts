type storage = {
  initaltacoz : tez;
  lastorder: address;
  owner: address;
  fa2contractaddress: address;
  fa2owneraddress:  address;  
}

let main : storage = {
    initaltacoz = 4000tez;
    lastorder = ("x" : address);
    owner = ("x" : address);
    fa2contractaddress = ("x" : address);
    fa2owneraddress = ("x" : address);
}